# Clinic — قائمة اختبار يدوي قبل الإطلاق

نفّذ هذه السيناريوهات فعليًا بعد ربط مشروع Supabase حقيقي وتطبيق كل الـ Migrations، بترتيب من الأعلى للأسفل. لا تعتبر المشروع جاهزًا للإطلاق قبل ما تعدّي عليها كلها.

## 1. Authentication

- [ ] تسجيل حساب جديد من `/register` بكل الحقول → يظهر role=USER في `profiles` تلقائيًا.
- [ ] محاولة التسجيل ببريد مستخدم بالفعل → رسالة عربية واضحة.
- [ ] تسجيل الدخول ببيانات صحيحة/خاطئة.
- [ ] نسيت كلمة المرور → وصول رابط فعلي بالبريد → تعيين كلمة مرور جديدة تعمل.
- [ ] تسجيل الخروج يمسح الجلسة فعليًا (Refresh لا يُبقيك مسجّلًا).

## 2. الأدوار الأربعة

- [ ] مستخدم USER يوصل لـ `/dashboard` وميقدرش يوصل لـ `/provider/dashboard` أو `/admin` (Redirect تلقائي).
- [ ] حساب DOCTOR/NURSE (بعد إنشائه من الأدمن) يقدر يدخل بس ميقدرش يسجّل حساب جديد بنفسه من `/register`.
- [ ] محاولة تعديل `role` مباشرة عبر أي شاشة تعديل بيانات → تُرفض (تحقّق من الـ Network tab إن أي محاولة PATCH على العمود ده ترجع خطأ من RLS/Trigger).

## 3. الحجز (Doctor)

- [ ] حجز موعد كامل من البداية للنهاية (تاريخ → وقت → مراجعة → تأكيد) → يظهر في "حجوزاتي" بحالة `pending`.
- [ ] الطبيب يشوف نفس الطلب في "الطلبات" ويقدر يقبل/يرفض.
- [ ] بعد القبول، المستخدم ياخد إشعار Realtime فوري (من غير Refresh).
- [ ] افتح نفس صفحة الحجز من متصفّحين/تابين مختلفين، واحجز نفس المعاد بالظبط من الاتنين بسرعة → واحد بس ينجح، التاني ياخد "عذرًا، هذا الموعد لم يعد متاحًا".

## 4. الحجز (Nurse + Maps)

- [ ] اختيار خدمة → تاريخ → وقت → تحديد موقع بكل الطرق الأربعة (موقعي الحالي، نقر، سحب، بحث) → إرسال طلب.
- [ ] الممرض يشوف عنوان الزيارة كاملًا في تفاصيل الطلب.
- [ ] فصل الإنترنت أثناء رفع الموقع (محاكاة فشل) → تأكد إن الحجز اتلغى تلقائيًا ومفيش "طلب معلَّق بلا عنوان".

## 5. آلة حالة الحجز

- [ ] تتبّع دورة كاملة: `pending → accepted → confirmed → in_progress → completed`.
- [ ] تأكد إن المريض ميقدرش "يقبل" حجزه بنفسه، والمزوّد ميقدرش "يلغي" حجز مريض بدل ما يرفضه.
- [ ] بعد `completed`، يظهر فورم التقييم للمريض، ومحاولة تقييم حجز `pending` تانٍ لازم تتمنع.
- [ ] تقييم نفس الحجز مرتين → يُمنع (unique constraint).

## 6. RLS (الأهم أمنيًا)

- [ ] بحساب USER عادي، حاول تجيب حجوزات مستخدم تاني بمعرفة الـ ID مباشرة عبر API — لازم ترجع فاضية.
- [ ] بحساب DOCTOR، حاول تشوف حجوزات دكتور تاني — لازم تترفض.
- [ ] بحساب DOCTOR، تأكد إنه يقدر يشوف اسم/هاتف مريض عنده حجز معاه بس، مش أي مستخدم تاني في النظام.
- [ ] تأكد إن Storage: رفع صورة في مجلد مستخدم تاني (`avatars/<غير معرفك>/...`) يترفض.

## 7. الإشعارات

- [ ] جرس الإشعارات بيحدّث العداد فوريًا عند وصول إشعار جديد.
- [ ] "تعليم كمقروء" و"تعليم الكل كمقروء" يشتغلوا فعليًا (تحقّق من `is_read` في القاعدة).

## 8. لوحة الأدمن

- [ ] إنشاء حساب طبيب جديد بالكامل (Edge Function لازم تكون منشورة) → يقدر يسجّل دخول فورًا.
- [ ] إيقاف حساب (طبيب/ممرض/مستخدم) → يتأكد إنه ميقدرش يسجّل دخول أو يستخدم المنصة وهو موقوف.
- [ ] كل عملية من العمليات دي تظهر تلقائيًا في "سجل العمليات".
- [ ] الإحصائيات (أكثر التخصصات، أفضل التقييمات) بتتغيّر فعليًا مع بيانات جديدة، مش أرقام ثابتة.

## 9. الموبايل

- [ ] كل الصفحات السابقة تتصفّح كاملة على شاشة 375px عرض بدون Horizontal Scroll.
- [ ] Bottom Navigation في لوحة المستخدم/مقدّم الخدمة شغّالة ومقروءة.
- [ ] الخريطة (Mapbox) قابلة للاستخدام باللمس (تكبير/تصغير/سحب العلامة) على الموبايل الفعلي.

## 10. عام

- [ ] `npm run build` ينجح بدون أخطاء.
- [ ] Console المتصفح خالي من أخطاء JavaScript/React غير متوقعة في أي صفحة من اللي فوق.
- [ ] كل شاشة بيانات (قوائم، تفاصيل) عندها Loading/Empty/Error state واضح ومُختبَر فعليًا (جرّب قطع الإنترنت لحظيًا لرؤية حالة الخطأ).
