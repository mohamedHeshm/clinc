/**
 * مكوّن مؤقت لهذه المرحلة (Project Setup) فقط.
 * كل صفحة هنا سيتم استبدالها بمحتوى حقيقي متصل بـ Supabase
 * في مرحلتها المخصصة حسب خطة التنفيذ في 01-architecture.md.
 * لا يظهر هذا المكوّن في النسخة النهائية للمشروع.
 */
export function ScaffoldPlaceholder({ label }: { label: string }) {
  return (
    <div className="flex min-h-[50vh] items-center justify-center">
      <p className="text-muted-foreground text-sm">{label} — قيد الإنشاء</p>
    </div>
  );
}
